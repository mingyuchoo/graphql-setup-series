{
  "name": "accounts",
  "scripts": {
    "clean": "rimraf dist",
    "dev": "nodemon 'src/index.ts' --exec 'ts-node' src/index.ts -e ts",
    "build": "npm run clean && tsc -p tsconfig.json src/*.ts",
    "start": "npm run build && node 'dist/index.js'",
    "typescript": "latest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/federation": "latest",
    "apollo-server": "latest",
    "graphql": "latest",
    "typescript": "latest"
  },
  "devDependencies": {
    "@types/graphql": "latest",
    "@types/node": "latest",
    "nodemon": "latest",
    "rimraf": "latest",
    "ts-node": "latest"
  }
}
